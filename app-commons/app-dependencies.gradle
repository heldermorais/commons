grails{
    plugins{


        compile project(':common-autoconfig')
        compile project(':common-autorun')
        compile project(':common-events')

        compile project(':common-springsec')

    }
}

